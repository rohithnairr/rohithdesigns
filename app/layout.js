// Import necessary dependencies
import Head from 'next/head';
import Navigation from './components/navbar'; // Adjust the path as needed
import './globals.css'; // Assuming this is the correct path to your global CSS file

// Define metadata for the entire application
const metadata = {
  title: 'Portfolio',
  description: 'Generated by create next app',
};

export default function RootLayout({ children }) {
  return (
    <>
      {/* Set HTML head properties using the 'Head' component */}
      <Head>
        <meta charSet="UTF-8" />
        <meta name="viewport" content="width=device-width, initial-scale=1.0" />
        <title>{metadata.title}</title>
        <meta name="description" content={metadata.description} />
        {/* You can add more metadata tags for SEO and social sharing here */}
      </Head>
      {/* Render the navigation component */}
      <Navigation />
      {/* Render the children (content) */}
      {children}
    </>
  );
}
